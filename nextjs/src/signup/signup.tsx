import React from 'react'

// "We think" as usual the NextJS will create a route for this but not as the Folder name we give as signup is correct but the file anme should be page.tsx but not signup.tsx
// testing go on "/signup"
const SignupPage = () => {
  return (
    <div>
        <h1>Signup</h1>
    </div>
  )
}

export default SignupPage