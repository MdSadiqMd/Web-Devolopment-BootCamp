import React from 'react';
import ReactDOM from 'react-dom';
import App from './5_App';
/*Props are used to change the dynamic elements of the card  */
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

