import React from 'react';
import ReactDOM from 'react-dom';
import AppContainer from './25_App';

ReactDOM.render(
  <React.StrictMode>
    <AppContainer />
  </React.StrictMode>,
  document.getElementById('root')
);
